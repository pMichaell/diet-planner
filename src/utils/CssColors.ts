export const checkMarkGreen = `hsl(${getComputedStyle(
  document.documentElement
).getPropertyValue("--clr-checkmark-green")})`;

export const creamyGreen = `hsl(${getComputedStyle(
  document.documentElement
).getPropertyValue("--clr-creamy-green")})`;

export const wrongRed = `hsl(${getComputedStyle(
  document.documentElement
).getPropertyValue("--clr-wrong-red")})`;
